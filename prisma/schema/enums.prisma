enum ActiveStatus {
    ACTIVATE
    SUSPEND
}

enum Role {
    CUSTOMER
    PROVIDER
    ADMIN
}

enum GearCondition {
    NEW
    EXCELLENT
    GOOD
    FAIR
}

enum RentalStatus {
    PLACED
    CONFIRMED
    PAID
    PICKED_UP
    RETURNED
    CANCELLED
}

enum PaymentProvider {
    STRIPE
    SSLCOMMERZ
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}
